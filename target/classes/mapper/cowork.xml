<?xml version="1.0" encoding="UTF-8"?>

<!--  
	DTD(Document Type Definition)
	- 작성한 문서가 어떤 문서인지에 대해서 정의해 놓은 것.
	- 하나의 시스템 내에서 사용할 XML 데이터의 구조를 정의하여 유효성을 점검 시 사용하는 태그.
-->

<!-- 마이바티스 문서에서 복사해옴. -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="memberDAO">
	<resultMap type="caldto" id="calMap">
		<result property="title" column="cal_title"/>
		<result property="start" column="cal_start_time"/>
		<result property="end" column="cal_end_time"/>
		<result property="allDay" column="cal_allday"/>
	</resultMap>
	
	<!-- *******************지수**********************-->
	<select id="address_deptList" resultType="departmentDto">
		select * from department
	</select>
	
	<!-- *******************세건**********************-->

	<!-- **************** hslee **************** -->
	<select id="memberLogin" resultType="memberDTO" parameterType="memberDTO">
		select * from member where mem_id = #{mem_id} and mem_pwd = #{mem_pwd}
	</select>
	<insert id="memberJoin" parameterType="memberDTO">
		<selectKey resultType="int" keyProperty="mem_no" order="BEFORE">
			select max(mem_no) from member
		</selectKey>
		insert into member (mem_no, mem_name, mem_id, mem_pwd, mem_position, mem_rank, dept_name, team_name, mem_email, mem_phone, mem_image)
		values (#{mem_no} + 1, #{mem_name}, #{mem_id}, #{mem_pwd}, #{mem_position}, #{mem_rank}, #{dept_name}, #{team_name}, #{mem_email}, #{mem_phone}, #{mem_image})
	</insert>
	<select id="memberDetail" resultType="memberDTO">
		select * from member where mem_id = #{mem_id}
	</select>
	<update id="memberEdit">
		update member set mem_pwd = #{mem_pwd}, mem_position = #{mem_position}, mem_rank = #{mem_rank}, dept_name = #{dept_name}, team_name = #{team_name}, mem_email = #{mem_email}, mem_phone = #{mem_phone}, mem_image = #{mem_image}
	</update>
	<delete id="memberDelete">
		delete from member where mem_id = #{mem_id}
	</delete>
	<select id="memberFindId" resultType="memberDTO">
		select mem_id from member where mem_name = #{mem_name} and mem_email = #{mem_email}
	</select>
	<select id="memberFindPwd" resultType="String">
		select mem_pwd from member where mem_name = #{mem_name} and mem_id = #{mem_id} and mem_email = #{mem_email}
	</select>
	<select id="idCheck" parameterType="String" resultType="Integer">
		select count(*) from member where mem_id = #{mem_id}
	</select>
 </mapper> 